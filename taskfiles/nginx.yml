version: 3

# env:

tasks:
  status:
    desc: Nginx ステータス
    cmd:
      - sudo systemctl status $NGX_SERVICE_NAME.service

  restart:
    desc: Nginx 再起動
    cmds:
      - sudo systemctl restart $NGX_SERVICE_NAME.service
